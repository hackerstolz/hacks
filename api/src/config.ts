const AppConfig = {
    database: {
        //connection: process.env.USER + '@postgres://localhost:5432/hacks'
        connectionString: 'postgres://hacks:hacks@localhost:5432/hacks'
    }
};

export default AppConfig;
